import { Counter } from "./components/Counter";
import "./App.css";

const App = (): React.ReactElement => {
  return (
    <div className='App'>
      <Counter />
    </div>
  );
};

export default App;
